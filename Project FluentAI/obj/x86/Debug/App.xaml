<Application
    x:Class="Project_FluentAI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_FluentAI">

    <Application.Resources>
        <ResourceDictionary>
            <!-- Acrylic Brush for the Sidebar -->
            <AcrylicBrush x:Key="NavigationViewExpandedPaneBackground"
                          BackgroundSource="HostBackdrop"
                          TintColor="{ThemeResource SystemAltHighColor}"
                          TintOpacity="0.6"
                          FallbackColor="{ThemeResource SystemAltHighColor}"/>

            <!-- Custom ListViewItem style to match Settings navigation items -->
            <Style x:Key="SystemControlBackgroundListLowBrush" TargetType="ListViewItem">
                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                <Setter Property="TabNavigation" Value="Local"/>
                <Setter Property="IsHoldingEnabled" Value="True"/>
                <Setter Property="Padding" Value="12,0,12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="MinHeight" Value="40"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Grid x:Name="ContentPresenterGrid" Background="{TemplateBinding Background}" Margin="0,2,0,2">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="ContentPresenterGrid" 
                                                                Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)" 
                                                                To="{ThemeResource SystemListLowColor}" Duration="0"/>
                                            </Storyboard>
                                            <VisualState.Setters>
                                                <Setter Target="ContentPresenterGrid.Background" Value="{ThemeResource SystemControlBackgroundListLowRevealBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="ContentPresenterGrid" 
                                                                Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)" 
                                                                To="{ThemeResource SystemListMediumColor}" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Selected">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="ContentPresenterGrid" 
                                                                Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)" 
                                                                To="{ThemeResource SystemListLowColor}" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                
                                <Rectangle x:Name="SelectionIndicator" 
                                           Fill="{ThemeResource SystemControlHighlightAccentBrush}" 
                                           Width="4" Height="24" 
                                           HorizontalAlignment="Left" 
                                           Visibility="Collapsed"/>
                                
                                <ContentPresenter x:Name="ContentPresenter"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Content="{TemplateBinding Content}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>

